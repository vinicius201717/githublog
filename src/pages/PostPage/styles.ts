import { styled } from "styled-components";

export const ProfilePostContainer = styled.main`
    width: 90rem;
`
export const ProfilePostContent = styled.section`
    width: 54rem;
    margin: 3rem auto;
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
`